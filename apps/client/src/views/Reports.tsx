import { StatelessWidget } from '@/components';

export default function Reports () {
    return (
        <StatelessWidget
            type="page"
            size="large"
            title="Reports"
        />
    );
}
