import StatelessWidget from '@/components/StatelessWidget';

const Reports = () => {
    return (
        <StatelessWidget type="page" size="large" title="Reports" />
    );
};

export default Reports;
