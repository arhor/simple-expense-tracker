plugins {
    id "com.github.node-gradle.node"
    id "idea"
}

node {
    version = property("app.version.node")
    download = true
}

idea {
    module {
        excludeDirs += files(
            "$projectDir/node_modules",
            "$projectDir/dist",
        )
    }
}

