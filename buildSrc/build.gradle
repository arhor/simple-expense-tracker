plugins {
    id "groovy-gradle-plugin"
}

repositories {
    gradlePluginPortal()
    maven { url "https://jitpack.io" }
}

dependencies {
    implementation "com.github.node-gradle:gradle-node-plugin:${project.property("versions.nodeGradlePlugin")}"
    implementation "com.github.arhor:java-dotenv-revised:${project.property("versions.javaDotenvRevised")}"
}
