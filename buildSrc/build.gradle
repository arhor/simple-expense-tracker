plugins {
    id "groovy-gradle-plugin"
}

repositories {
    gradlePluginPortal()
}

dependencies {
    implementation "com.github.node-gradle:gradle-node-plugin:${property("app.version.nodeGradlePlugin")}"
}
